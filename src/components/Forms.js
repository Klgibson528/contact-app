import React, { Component } from "react";
import OnChangeFun from "./components/OnChangeFun.js";

function Forms(props) {
  <input
    className="input"
    type="text"
    name="name"
    value={this.state.name}
    onChange= OnChangeFun
  />;
}

export default Forms;
