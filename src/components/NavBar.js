import React, { Component } from "react";

function NavBar(props) {
  return (
    <nav className="App-header navbar-dark bg-primary">
      <h1>Contact List</h1>
    </nav>
  );
}

export default NavBar;
